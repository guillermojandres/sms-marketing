{% extends ':Layout:layout2.html.twig' %}
{% block css_extend %}        
    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="//cdnjs.cloudflare.com/ajax/libs/material-design-lite/1.1.0/material.min.css">
    <link rel="stylesheet" href="https://cdn.datatables.net/1.10.11/css/dataTables.material.min.css"> 
{% endblock %}

{% block descripcion_y_acciones %}
   
    <div class="row">
 {% block descripcion %}
        <div class="col-md-12">
            <p class="robo" style="font-weight: 300; margin-bottom: 0px; font-size: 30px;">CRM</p>
            <p class="robo" style="font-weight: 300; font-size: 14px; height: 40px;">Panel adminstrativo CRM</p>
        </div>
        <div class="clearfix"></div>
 {% endblock%}
 {% block acciones %}
         
 {% endblock %}
		
    </div>

{% endblock %}
{% block navegacion_Y_body %}
    <div class="row">
    {% block navegacion %}
        <hr style="margin-top: 0px !important;">
        <div class="col-md-2" style="border-right: 1px solid #f3f3f3;">
            <!-- Left Navigation -->
            <ul style="list-style: none; padding: 0;">
                <a href="{{ path('crm_dashbord') }}" class="leftnava"><li class="leftnav left active">Menu<span class="pull-right right"></span></li></a>
                <a href="{{ path('crm_configuracion') }}" class="leftnava"><li class="leftnav left">Configuraci&oacute;n</li></a>
                <a href="{{ path('crm_reportes') }}" class="leftnava"><li class="leftnav left">Reportes</li></a>
                <a href="{{ path('inventariobeard_listprod') }}" class="leftnava"><li class="leftnav left">Inventario</li></a>
            </ul>
        </div>
{% endblock %}

{% block cuerpo %}
    
    {#<div class="row">#}
    <div class="col-lg-10">
        <form class="navbar-form" role="search" action="{{ path('inventariobeard_newprod') }}" method="POST">
            <p class="text-center">LISTA DE PRODUCTOS</p>
            <button type="submit" class="btn btn-default">Nuevo producto</button>
            <a id="eliminarP" class="btn btn-default">Eliminar</a>
            <table id="example" class="mdl-data-table" cellspacing="0" width="100%">
                <thead>
                    <tr>
                        <th class="hidden">Id</th>
                        <th>Nombre</th>
                        <th>Precio</th>
                        <th>Existencias</th>
                        <th>Presentacion</th>                         
                    </tr>
                </thead>                
                <tbody>
                    {% for productos in productos %}
                    <tr>
                        <td class="hidden">{{productos.id}}</td>
                        <td>{{productos.nombre}}</td>
                        <td>{{productos.precio}}</td>
                        <td>{{productos.stock}}</td>
                        <td>{{productos.presentacion}}</td>                        
                    </tr>                    
                    {% endfor %}                                                                             
                </tbody>
            </table>
        </form>
    </div> 
    {#</div>#}
    <div style="height: 10px;"></div>
{% endblock %}

        </div>
{% endblock %}
 {% block js_extend %}
     
     <script src="https://cdn.datatables.net/1.10.11/js/jquery.dataTables.min.js"></script>
     <script src="https://cdn.datatables.net/1.10.11/js/dataTables.material.min.js "></script>
     
 
      <script>
        $(document).ready(function() {
            $("#eliminarP").hide();  
            var table = $('#example').DataTable( {
                columnDefs: [
                    {
                        targets: [ 0, 1, 2 ],
                        className: 'mdl-data-table__cell--non-numeric'
                    }
                ]
                } );

            //esto es para seleccionar un registro
            var idEliminar=0;

            $(document).on("click","tr",function() {

                var idProducto =  $(this).children().html();
                //alert(idProducto);
                $("tr").css('background-color', 'white');
                $(this).css('background-color', '#E9E6E6');

                $("#eliminarP").show();   
                idEliminar=idProducto;                                 
        });

        
   $("#eliminarP").on("click",function (){
    alert("asdadas");
            var valor = new Array();                                     
            valor.push(idEliminar);


//Funcion del POST que me permite inabilitar un Proveedor

                    /*$.post( "{{path('inventariobeard_delprod')}}", { idproducto: valor }, function( data ) {
                        var par = new Array();
                        par = data.query;
                                          
                        if(data === 0) {
                            
                            //var table = $('#example').DataTable();
                                 $("#eliminarP").hide();
                            table.ajax.reload('payment_mode');                                                      
                        }
                        else{
                                Lobibox.notify("error", {
                                          size: 'mini',
                                          msg: 'Error al elmonar los datos, espere un momento e intenta de nuevo'
                                      });
                                            location.reload();
                        }                       
                    }, "json");*/
                $.ajax({
                    data:{idproducto:valor},
                    url:"{{path('inventariobeard_delprod')}}",
                    type: "post",
                    async: false,
                    dataType: 'json',
                    success: function (data) {
                            var table = $('#example').DataTable();  
                            table.ajax.reload();                                                      
                        },
                    error: function (data) {
                        alertify.error("{#Hubo un error, intente de nuevo!#}");
                        bootbox.alert("Hubo un error, intente de nuevo!", function() {});
                    }
                });
    
               
               
   });






            $(document).on("dblclick","tr",function() {
                var idProducto =  $(this).children().html();

                var numero = idProducto;
                //var url=Routing.generate('editarClientes',{id:numero});
                window.open(url,"_self");
                $("#eliminarCP").hide();   
            });            
        });
      </script>
            
  {% endblock %} 


