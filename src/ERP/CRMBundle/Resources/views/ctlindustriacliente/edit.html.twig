{% extends 'base.html.twig' %}

{% block body %}
   

{#    {{ form_start(edit_form, {'action': path('industria_edit',{'id':ctlIndustriaCliente.id})}) }}
        {{ form_widget(edit_form) }}
        
        <input type="submit" value="Edit" class="btn btn-success btn-sm" style="margin-top:10px; display: none; " id="submitEditar"  />
    {{ form_end(edit_form) }}#}

    <div class="row">
    {{ form_start(edit_form, {'action': path('edit_industria',{'id':ctlIndustriaCliente.id})}) }}
        
        {#{{ form_widget(form) }}#}
        
        <div class="col-md-4">
            {{ form_row(edit_form.descripcion, {'attr': {'class': 'form-control industriaTextBoxEdit '} }) }} 
        </div>
        <input type="submit" value="Guardar" class="btn btn-success btn-sm" style="margin-top:10px; display: none;" id="submitInsertar" />
        
    {{ form_end(edit_form) }}
    
    </div>
    <hr>
{% endblock %}
