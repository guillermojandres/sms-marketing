{% extends 'base.html.twig' %}

{% block body %}
   {# <h4>Insercion de territorio</h4>
   #}
      <div class="row">
    {{ form_start(form, {'action': path('industriacliente_new')}) }}
        
        {#{{ form_widget(form) }}#}
        
        <div class="col-md-4">
            {{ form_row(form.descripcion, {'attr': {'class': 'form-control industriaTextBox '} }) }} 
        </div>
        <input type="submit" value="Guardar" class="btn btn-success btn-sm" style="margin-top:10px; display: none;" id="submitInsertar" />
        
    {{ form_end(form) }}
    
    </div>
    <hr>
    
    
{% endblock %}
