{% extends 'base.html.twig' %}

{% block body %}
    <h1>CtlDepartamentoEmpresa</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ctlDepartamentoEmpresa.id }}</td>
            </tr>
            <tr>
                <th>Departamentoempresacol</th>
                <td>{{ ctlDepartamentoEmpresa.departamentoEmpresacol }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('ctldepartamentoempresa_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('ctldepartamentoempresa_edit', { 'id': ctlDepartamentoEmpresa.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
