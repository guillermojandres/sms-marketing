{% extends 'base.html.twig' %}

{% block body %}
    <h1>InvProveedor</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ invProveedor.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ invProveedor.nombre }}</td>
            </tr>
            <tr>
                <th>Direccion</th>
                <td>{{ invProveedor.direccion }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_invproveedor_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_invproveedor_edit', { 'id': invProveedor.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
