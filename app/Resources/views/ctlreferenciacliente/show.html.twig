{% extends 'base.html.twig' %}

{% block body %}
    <h1>CtlReferenciaCliente</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ctlReferenciaCliente.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ ctlReferenciaCliente.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_referenciacliente_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_referenciacliente_edit', { 'id': ctlReferenciaCliente.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
