{% extends 'base.html.twig' %}

{% block body %}
    <h1>RhTipoIngreso</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rhTipoIngreso.id }}</td>
            </tr>
            <tr>
                <th>Nombreingresocol</th>
                <td>{{ rhTipoIngreso.nombreIngresocol }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ rhTipoIngreso.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('rhtipoingreso_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('rhtipoingreso_edit', { 'id': rhTipoIngreso.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
