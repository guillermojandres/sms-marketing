{% extends 'base.html.twig' %}

{% block body %}
    <h1>CtlEstado</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ctlEstado.id }}</td>
            </tr>
            <tr>
                <th>Nombreestado</th>
                <td>{{ ctlEstado.nombreEstado }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('ctlestado_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('ctlestado_edit', { 'id': ctlEstado.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
