{% extends 'base.html.twig' %}

{% block body %}
    <h1>RhEstructuraSalarios</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rhEstructuraSalario.id }}</td>
            </tr>
            <tr>
                <th>Fechainicio</th>
                <td>{{ rhEstructuraSalario.fechaInicio }}</td>
            </tr>
            <tr>
                <th>Fechafin</th>
                <td>{{ rhEstructuraSalario.fechaFin }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('rhestructurasalarios_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('rhestructurasalarios_edit', { 'id': rhEstructuraSalario.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
